// Shown when a card cannot be produced — which usually means no signal, not a missing part.
import { CardNotFound } from '@/components/CardNotFound'

export default function ComponentCardNotFound() {
  return <CardNotFound />
}
